console.log("APP.js is running!")

// JSX - Javascript XML
var template = React.createElement("p", null, " This is a JSX test app.js! ");
var appRoot = document.getElementById('app');

ReactDOM.render(template, appRoot);

//Babel is javascript compiler
